name: Python

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: python-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.11', '3.12']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build pkg-config libgsl-dev libeigen3-dev

      - name: Install package and test dependencies
        run: |
          python -m pip install --upgrade pip build
          python -m pip install -e "./Hyperiso/Hyperiso[test]"

      - name: Check MARTY template mirrors
        run: python tools/check_marty_template_sync.py

      - name: Validate MARTY v1.0.3 regressions
        run: python tools/test_marty_v103.py --static-only

      - name: Import smoke test
        run: |
          python - <<'PY'
          import pyhyperiso
          from pyhyperiso.phyperiso import pyhyperiso as native

          assert pyhyperiso.__version__ == native.__version__
          print(f'pyhyperiso {pyhyperiso.__version__} import OK')
          PY

      - name: Run Python tests
        run: python -m pytest Hyperiso/Hyperiso/pyhyperiso/test -q --import-mode=importlib

      - name: Run database-writer Python example
        run: |
          python Hyperiso/Hyperiso/examples_python/Core/database_writer_example.py \
            --output-dir "$PWD/database_exports_python"
          test -s database_exports_python/database.json
          test -s database_exports_python/database.yaml
          test -s database_exports_python/database.flha
          grep -q '^BLOCK MASS' database_exports_python/database.flha

      - name: Test reproducibility tooling
        run: python -m unittest discover -s reproducibility/tests -v

      - name: Test GHyperiso plotting helpers
        run: |
          python -m pip install -r GHyperiso/HyperisoDashGUI/requirements.txt
          PYTHONPATH=GHyperiso/HyperisoDashGUI \
            python -m pytest GHyperiso/HyperisoDashGUI/test -q

      - name: Build source distribution and wheel
        run: python -m build ./Hyperiso/Hyperiso
