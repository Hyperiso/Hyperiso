[metadata]
name = phyperiso
version = 0.0.1

[options]
packages = find:
python_requires = >=3.7

[options.packages.find]
where = .
include = pyhyperiso*
exclude = tests*

[tool.scikit-build]
cmake-source-dir = ../core/Python
cmake-install-dir = pyhyperiso/phyperiso
